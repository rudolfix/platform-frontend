import * as React from "react";

import { Button } from "reactstrap";

export const Start: React.SFC = () => (
  <div>
    <h1>Start</h1>
    <Button color="primary">I am a person</Button>
    <Button color="primary">I am a company</Button>
  </div>
);
