import * as React from "react";
import { Text } from "react-native";

/**
 * A line break (a kind of `br` tag alternative for react-native).
 */
const LineBreak: React.FunctionComponent = () => <Text>{"\n"}</Text>;

export { LineBreak };
