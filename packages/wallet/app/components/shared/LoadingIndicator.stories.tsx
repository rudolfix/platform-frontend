import { storiesOf } from "@storybook/react-native";
import * as React from "react";

import { LoadingIndicator } from "./LoadingIndicator";

storiesOf("Atoms|LoadingIndicator", module).add("default", () => <LoadingIndicator />);
