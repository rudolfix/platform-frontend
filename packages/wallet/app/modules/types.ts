/**
 * Represents common screen states that are used across app screens
 */
export enum EScreenState {
  INITIAL = "initial",
  LOADING = "loading",
  REFRESHING = "refreshing",
  READY = "ready",
  ERROR = "error",
}
