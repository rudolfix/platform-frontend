import { storiesOf } from "@storybook/react";
import * as React from "react";

import { DatePickerLayout } from "./DatePickerLayout";

storiesOf("DatePickerLayout", module).add("default", () => <DatePickerLayout />);
