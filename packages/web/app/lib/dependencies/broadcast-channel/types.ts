export declare type UserActivityChannelMessage = {
  status: EUserActivityMessage;
};

export enum EUserActivityMessage {
  USER_ACTIVE = "user-active",
  // Add here more inter tab user activities
}
